const diaporama = new Diaporama($('.diapo')[0], 5000);
const book = new Bookform($('#book')[0]);
const booking = new Booking($('#booking')[0]);
const map = new Map('map');
const signature = new Signature($('#signature')[0]);